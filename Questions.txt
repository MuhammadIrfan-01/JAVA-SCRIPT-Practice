1. Find the Largest Number in an Array Write a function that loops through an array of numbers and retums the largest value.

2. Count the Even Numbers
   Given an array of integers, count how many of them are even using a loop.

3. Reverse an Array (without using .reverse())
   Use a loop to create a new array containing elements of the original array in reverse order.

4. Remove Duplicates From an Array Write a function that returns a new array with duplicates removed (without using Set).

5. Sum of All Elements
   Given an array of number calculate their total using a loop.
